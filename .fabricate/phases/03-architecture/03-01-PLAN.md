---
phase: 3
plan: 1
title: "Architecture from Intelligence -- Wails Scaffold + Engine + Tools"
status: ready

must_haves:
  truths:
    - "Wails v2 application starts and renders a two-pane window on Windows"
    - "User can type a message, send it to Gemini, and see a streamed response"
    - "Gemini streaming response appears incrementally in the chat pane"
    - "Tool calls are dispatched, executed, and results returned to the model"
    - "Session persists conversation history across application restarts"
    - "Context budget is enforced -- observation masking reduces large tool outputs"
    - "Model routing selects Pro vs Flash based on task classification"
    - "OAuth sign-in flow works from the desktop application"
    - "All unit tests pass with go test ./internal/..."
    - "Test coverage >= 80% for new packages"

  artifacts:
    - file: "cmd/gkestral/main.go"
      min_lines: 30
      contains: "wails.Run"
    - file: "internal/engine/engine.go"
      min_lines: 100
      contains: "Engine"
    - file: "internal/engine/conversation.go"
      min_lines: 80
      contains: "Conversation"
    - file: "internal/engine/context.go"
      min_lines: 60
      contains: "ContextBudget"
    - file: "internal/engine/tools.go"
      min_lines: 80
      contains: "ToolRegistry"
    - file: "internal/engine/session.go"
      min_lines: 80
      contains: "SessionStore"
    - file: "internal/tools/filesystem.go"
      min_lines: 60
      contains: "ReadFile"
    - file: "internal/tools/terminal.go"
      min_lines: 40
      contains: "RunCommand"
    - file: "internal/tools/research.go"
      min_lines: 40
      contains: "SearchGrounding"
    - file: "internal/workspace/workspace.go"
      min_lines: 60
      contains: "Workspace"
    - file: "frontend/src/main.js"
      min_lines: 10
      contains: "Greet"
    - file: "go.mod"
      contains: "wails"

  key_links:
    - pattern: "NewEngine"
      file: "internal/engine/engine.go"
    - pattern: "import.*engine"
      file: "cmd/gkestral/main.go"
    - pattern: "import.*gemini"
      file: "internal/engine/engine.go"
    - pattern: "import.*tools"
      file: "internal/engine/tools.go"
    - pattern: "ToolRegistry"
      file: "internal/engine/engine.go"
---

<acceptance_criteria>
AC-1: Given Wails v2 is installed
      When `wails dev` is run in the project root
      Then a native window opens with a two-pane layout
      And the left pane shows a chat input area
      And the right pane shows an empty artifact space

AC-2: Given the application is running
      When the user types a message and presses Enter
      Then the message is sent to Gemini via the streaming API
      And response tokens appear incrementally in the chat pane
      And the complete response is stored in conversation history

AC-3: Given a conversation with tool declarations registered
      When Gemini returns a function call (e.g., read_file)
      Then the tool registry dispatches to the correct handler
      And the tool result is returned to Gemini for the next turn
      And the tool execution is visible in the chat pane

AC-4: Given a conversation history with 5+ turns
      When the application is closed and reopened
      Then the previous session is loaded from SQLite
      And the conversation history is displayed in the chat pane
      And the user can continue the conversation

AC-5: Given a tool execution that returns >10,000 characters
      When the result is injected back into the conversation
      Then observation masking reduces the content to a summary
      And the full output is available on demand (expandable)
      And the context budget tracker shows token usage

AC-6: Given a task classified as "deep reasoning" by the user prompt
      When the engine processes the request
      Then gemini-3.1-pro-preview is selected with thinking enabled
      And for "fast extraction" tasks, gemini-3.1-flash is selected

AC-7: Given the user has not signed in with Google
      When the user clicks "Sign in with Google" in the application
      Then the OAuth desktop PKCE flow runs (browser opens, consent, callback)
      And tokens are stored securely
      And the user's email is displayed in the UI

AC-8: Given all internal/ packages
      When go test ./internal/... is run
      Then all tests pass (including engine, tools, workspace)
      And coverage for new packages >= 80%

AC-9: Given the engine is configured with a Gemini API key
      When a search-grounded query is sent
      Then the research tool is invoked
      And citations are extracted and available in the response

AC-10: Given the workspace model
       When the application is opened in a directory
       Then the workspace is detected (project name, type, file tree)
       And workspace metadata is available to the engine for context

AC-11: Given a tool call that requires user approval (e.g., write_file, run_command)
       When the engine dispatches the tool call
       Then execution pauses and the UI displays an approval prompt
       And execution resumes only after the user explicitly approves or denies
       And denied tools return a descriptive message to the model (not an error)
</acceptance_criteria>

<boundaries>
Protected files (do not modify):
- internal/gemini/*.go (Phase 01 deliverable -- read-only)
- internal/google/*.go (Phase 02 deliverable -- read-only)
- .fabricate/references/* (research documents)
- website/* (marketing site)
- docs/gemini-mastery-reference.md (Phase 01 doc)
- docs/google-api-strategy.md (Phase 02 doc)
- docs/prompt-engineering-playbook.md (Phase 02 doc)

Scope limits:
- No rich UI components (Phase 04 -- basic HTML/CSS only for scaffold)
- No multimodal rendering (images, diagrams, maps -- Phase 04)
- No DOCX/PPTX generation (Phase 04)
- No MCP client (v0.2+)
- No plugin system (v0.2+)
- No Stitch/NotebookLM integration (v0.2+)
- Frontend is minimal scaffold -- proves the architecture works
- No CI/CD pipeline (separate concern)

Allowed to read from Phase 01/02:
- Import and use internal/gemini/* and internal/google/* packages
- Reference docs/ for architecture decisions
</boundaries>

<test_plan>
## Test Strategy (T1)

### Unit Tests
- Engine: mock Gemini client, test conversation flow, tool dispatch, context budget
- Tools: mock filesystem, test read/write/search, command approval flow
- Workspace: mock filesystem, test project detection, metadata extraction
- Session: SQLite in-memory for tests, test save/load/list sessions

### Integration Tests (build-tag gated)
- Full engine -> Gemini streaming (needs API key)
- Engine -> tool dispatch -> filesystem (real files in temp dir)
- Session persistence round-trip (SQLite on disk)
- Wails build test (`wails build` succeeds)

### Coverage Target
- internal/engine/ >= 80%
- internal/tools/ >= 80%
- internal/workspace/ >= 80%
- internal/gemini/ maintained >= 85%
- internal/google/* maintained at current levels

### Verification Commands (wave-aware)
```bash
go vet ./...
go test ./internal/...
go test -cover ./internal/engine/
go test -cover ./internal/tools/
go test -cover ./internal/workspace/
```
</test_plan>

<checkpoints>
After task T2: checkpoint:human-verify
  Description: Commander verifies Wails application starts, renders two-pane layout, and accepts keyboard input on Windows.
  Blocking: true

After task T11: checkpoint:human-verify
  Description: Commander verifies end-to-end flow: type message -> streamed Gemini response -> tool call -> result displayed.
  Blocking: true
</checkpoints>

<handshake_protocol>
AR-3 Three-Way Handshake (mandatory for T1):

Every 2-3 waves, parallel three-way code review:
1. Gemini Deep Think -- architecture coherence, context engine design, cache strategy
2. Codex CLI (gpt-5.4) -- package boundaries, error handling, test coverage
3. Minimax M2.7 -- Go idioms, goroutine safety, resource management

The architecture phase is the most critical for handshake review --
design mistakes here compound in Phase 04.
</handshake_protocol>

<tasks>
<!-- ============================================================ -->
<!-- Wave 1: Scaffold (Wails project, frontend, workspace)       -->
<!-- ============================================================ -->

<task id="T1" wave="1" depends_on="" complexity="moderate">
  <files>go.mod, wails.json, cmd/gkestral/main.go, cmd/gkestral/app.go</files>
  <action>
    Install Wails v2 and initialise the project scaffold:

    - Install: `go install github.com/wailsapp/wails/v2/cmd/wails@latest`
    - Do NOT run `wails init` (would overwrite existing go.mod)
    - Manually add Wails dependency: `go get github.com/wailsapp/wails/v2`
    - Create wails.json configuration:
      * name: "Gkestral"
      * outputfilename: "gkestral"
      * frontend:install: "npm install"
      * frontend:build: "npm run build"
      * frontend:dev:watcher: "npm run dev"
      * assetdir: "frontend/src" (Wails v2 embeds via //go:embed all:frontend/src)
    - Create cmd/gkestral/main.go (BOOTSTRAP ONLY -- frozen after this task):
      * Import wails v2
      * //go:embed all:frontend/src for asset embedding
      * wails.Run() with options referencing App struct from app.go
      * This file is NOT edited by T11 or T13 (file ownership frozen)
    - Create cmd/gkestral/app.go (ALL Wails-bound methods live here):
      * App struct with Wails lifecycle methods (startup, shutdown)
      * Configure window: title "Gkestral", width 1400, height 900
      * Placeholder Greet(name string) string method for initial frontend test
      * Bind App methods for frontend access
    - Add modernc.org/sqlite dependency for session persistence (pure Go, no CGO)

    Deferred to later milestones (explicit T1 gate deferrals):
    - CI/CD pipeline (needs CGO for race detection on Windows)
    - Signed binary distribution
    - User-facing documentation
    - Eval coverage suite
    - MCP client protocol

    This task creates main.go (bootstrap, frozen) + app.go (all methods). Frontend in T2.
  </action>
  <verify>go build ./cmd/gkestral/ && echo "BUILD OK"</verify>
  <done>Wails app entry point compiles with bound App struct and main.go/app.go separation</done>
</task>

<task id="T2" wave="1" depends_on="" complexity="simple">
  <files>frontend/package.json, frontend/src/main.js, frontend/src/index.html, frontend/src/style.css</files>
  <action>
    Create minimal frontend for two-pane layout:

    - frontend/package.json with vanilla JS (no framework initially)
      * No build step needed -- Wails v2 embeds frontend/src/ directly
        via `//go:embed all:frontend/src` in main.go (assetdir in wails.json)
      * "install": "echo no-op" (no npm dependencies for vanilla scaffold)
    - frontend/src/index.html:
      * Two-pane CSS grid layout (left 60%, right 40%)
      * Left pane: chat area (message list + input box)
      * Right pane: artifact space (empty placeholder)
      * Top bar: app title, model indicator, sign-in button
      * Dark theme by default (CSS variables for theming)
    - frontend/src/style.css:
      * CSS custom properties for colours (dark theme)
      * Grid layout for two panes
      * Chat message styling (user vs assistant)
      * Input area with send button
      * Monospace font for code, proportional for prose
    - frontend/src/main.js:
      * Import Wails runtime from '/wails/runtime.js' (auto-injected by Wails dev server)
      * Call Go methods via `window.go.main.App.MethodName()` -- this is the
        actual Wails v2.11.0 binding pattern (NOT wails.Call() which does not exist)
      * Listen for Go events via `runtime.EventsOn("event-name", callback)`
      * Wire send button to `window.go.main.App.Greet(name)` as smoke test
      * Basic keyboard handling (Enter to send, Shift+Enter for newline)

    Minimal -- proves the Wails bridge works. No React/Svelte/Vue.
  </action>
  <verify>ls frontend/src/index.html frontend/src/main.js frontend/src/style.css && echo "FILES OK"</verify>
  <done>Two-pane layout renders with Wails v2 binding pattern (window.go.main.App.*)</done>
</task>

<task id="T3" wave="1" depends_on="" complexity="simple">
  <files>internal/workspace/workspace.go, internal/workspace/workspace_test.go</files>
  <action>
    Build workspace detection and metadata:

    - Workspace struct: RootDir, Name, Type (git/plain/unknown), Files []string
    - DetectWorkspace(dir string) (*Workspace, error):
      * Check for .git -> type "git"
      * Extract project name from directory name
      * List top-level files (no deep recursion)
    - WorkspaceContext(ws *Workspace) string:
      * Produce a text summary for Gemini context injection:
        "Working directory: {name}\nType: {type}\nFiles: {list}"
    - FileTree(ws *Workspace, maxDepth int) string:
      * Produce indented file tree (up to maxDepth levels)
      * Exclude common patterns: .git, node_modules, __pycache__, .DS_Store

    Test with temp directories containing mock project structures.
  </action>
  <verify>go test -v ./internal/workspace/ && echo "WORKSPACE TESTS OK"</verify>
  <done>Workspace detection works for git and plain directories</done>
</task>

<!-- ============================================================ -->
<!-- Wave 2: Foundation Types (conversation + context -- BEFORE   -->
<!-- engine). T5/T6 define types that T4 wires together.         -->
<!-- Wave 4: Engine Core (T4 wires T5+T6+T7, parallel with T8/T9) -->
<!-- ============================================================ -->

<task id="T4" wave="4" depends_on="T5,T6,T7" complexity="complex">
  <files>internal/engine/engine.go, internal/engine/engine_test.go</files>
  <action>
    Build the core conversation engine that WIRES together foundation types.

    T5 (Conversation), T6 (ContextBudget), and T7 (ToolRegistry) must
    already exist. This task composes them -- it does NOT define new types
    that belong in those packages.

    - Engine struct:
      * geminiClient *gemini.Client
      * router *gemini.Router
      * toolRegistry *ToolRegistry       (from T7)
      * conversation *Conversation       (from T5)
      * contextBudget *ContextBudget     (from T6)
      * sessionStore SessionStore
      * workspace *workspace.Workspace
      * config EngineConfig
    - EngineConfig: APIKey, DefaultModel, MaxContextTokens, ThinkingLevel
    - NewEngine(config EngineConfig, opts ...EngineOption) *Engine
      * WithToolRegistry, WithSessionStore, WithWorkspace
    - Send(ctx, message string) (<-chan StreamEvent, error):
      * Run in goroutine (CRITICAL: prevents Wails IPC deadlock)
      * Build conversation history via Conversation.ToMessages()
      * Apply context budget via ContextBudget.ObservationMask()
      * Select model via router
      * Build system prompt via SystemPromptBuilder (from T6)
      * Stream response via gemini.StreamResponse
      * Handle tool calls: dispatch via ToolRegistry -> collect results -> re-send
      * Return channel of StreamEvent (text, tool_call, tool_result, done, error)
    - StreamEvent: Type (text/tool_call/tool_result/thinking/done/error),
      Text, ToolCall, Error

    This is the central orchestration point. Tests use a mock Gemini client
    that returns pre-programmed responses (text, tool calls, errors).
  </action>
  <verify>go test -v ./internal/engine/ -run TestEngine && echo "ENGINE TESTS OK"</verify>
  <done>Engine sends messages, streams responses, handles tool calls</done>
</task>

<task id="T5" wave="2" depends_on="" complexity="moderate">
  <files>internal/engine/conversation.go, internal/engine/conversation_test.go</files>
  <action>
    Build conversation/turn management:

    - Turn struct: Role (user/model/tool), Content string, ToolCalls []ToolCall,
      ToolResults []ToolResult, Timestamp time.Time, TokenCount int
    - Conversation struct: ID, Turns []Turn, CreatedAt, UpdatedAt, Title
    - NewConversation() *Conversation
    - AddUserTurn(content string) -- append user message
    - AddModelTurn(content string, toolCalls []ToolCall) -- append model response
    - AddToolResult(callID, name, result string) -- append tool result
    - ToMessages() []gemini.Message -- convert to Gemini API format
    - EstimateTokens() int -- rough token count (chars/4 heuristic)
    - TruncateToFit(maxTokens int) -- drop oldest turns to fit budget
    - GenerateTitle() string -- extract title from first user message

    Test: round-trip conversion, token estimation, truncation.
  </action>
  <verify>go test -v ./internal/engine/ -run TestConversation && echo "CONVERSATION TESTS OK"</verify>
  <done>Conversation manages turns and converts to Gemini message format</done>
</task>

<task id="T6" wave="2" depends_on="" complexity="moderate">
  <files>internal/engine/context.go, internal/engine/context_test.go</files>
  <action>
    Build context budget management with observation masking:

    - ContextBudget struct: MaxTokens, SystemTokens, HistoryTokens, ToolResultTokens
    - NewContextBudget(maxTokens int) *ContextBudget
    - AllocateSystem(tokens int) -- reserve for system prompt
    - AllocateHistory(tokens int) -- reserve for conversation history
    - Remaining() int -- available tokens
    - Usage() ContextUsage -- breakdown of token allocation
    - ObservationMask(content string, maxTokens int) (masked string, newTokenCount int):
      * If content fits within maxTokens: return as-is with current token estimate
      * If content exceeds maxTokens: summarise
        - Keep first 500 chars + last 500 chars
        - Add "[...{N} characters omitted, use expand to see full output...]"
        - Return BOTH masked content AND recalculated token count
        - Caller MUST update Turn.TokenCount with the returned value
      * JetBrains research: 50%+ cost reduction with equivalent solve rates
    - SystemPromptBuilder struct:
      * AddRole(role string)
      * AddWorkspaceContext(ws *workspace.Workspace)
      * AddToolDeclarations(tools []ToolDeclaration)
      * AddConstraints(constraints []string)
      * Build() string -- produces the final system prompt
      * EstimateTokens() int

    Test: budget allocation, overflow handling, observation masking,
    system prompt construction.
  </action>
  <verify>go test -v ./internal/engine/ -run TestContext && echo "CONTEXT TESTS OK"</verify>
  <done>Context budget enforced, observation masking reduces large outputs</done>
</task>

<!-- ============================================================ -->
<!-- Wave 3: Tool Registry (wraps Phase 01 types, adds approval) -->
<!-- Wave 4 cont: Built-in tools (T8/T9 parallel with T4)       -->
<!-- ============================================================ -->

<task id="T7" wave="3" depends_on="T6" complexity="moderate">
  <files>internal/engine/tools.go, internal/engine/tools_test.go</files>
  <action>
    Build the tool registry and dispatch system.

    IMPORTANT: Wrap Phase 01 types from internal/gemini/tools.go rather than
    duplicating. Phase 01 already has FunctionDeclaration and dispatch patterns.

    - ToolDeclaration: wraps gemini.FunctionDeclaration (adds RequiresApproval flag)
    - ToolHandler: func(ctx context.Context, args map[string]any) (string, error)
    - ToolRegistry struct: tools map[string]registeredTool
    - registeredTool: Declaration ToolDeclaration, Handler ToolHandler,
      RequiresApproval bool
    - NewToolRegistry() *ToolRegistry
    - Register(decl ToolDeclaration, handler ToolHandler, opts ...ToolOption)
    - ToolOption: WithApproval(bool)
    - Declarations() []gemini.FunctionDeclaration -- unwrap for Gemini API tool array
    - ApprovalFunc: func(callID, toolName string, args map[string]any) (bool, error)
      * Injected by Wails layer -- emits approval event, blocks on channel
      * Returns true (approved) or false (denied)
      * MUST have a timeout (default 5 minutes) to prevent orphaned goroutines
      * On timeout: return false, nil (deny with cleanup)
    - Dispatch(ctx, name string, args map[string]any) (string, error):
      * Look up handler
      * If RequiresApproval AND ApprovalFunc != nil:
        - Call ApprovalFunc (blocks until user responds or timeout)
        - If denied: return "User denied permission for {tool}" (not an error --
          model receives this as tool result and can adjust)
      * Execute handler
      * Apply observation masking if result is large (via ContextBudget.ObservationMask)
    - DispatchParallel(ctx, calls []gemini.ToolCall) ([]ToolResult, error):
      * Dispatch multiple tool calls concurrently
      * SAFETY: Only dispatch read-only tools in parallel; side-effecting tools
        (write_file, run_command) must be serialised to avoid race conditions
      * Collect results, match by call ID

    Test with mock tools: register, dispatch, parallel dispatch (read-only),
    serial dispatch (side-effecting), approval flow with timeout,
    unknown tool error, large output masking.
  </action>
  <verify>go test -v ./internal/engine/ -run TestTool && echo "TOOL TESTS OK"</verify>
  <done>Tool registry dispatches to handlers with approval support</done>
</task>

<task id="T8" wave="4" depends_on="T3,T7" complexity="moderate">
  <files>internal/tools/filesystem.go, internal/tools/filesystem_test.go</files>
  <action>
    Build filesystem tools for the engine:

    - ReadFile(ctx, args) (string, error):
      * args: {"path": string, "start_line": int, "end_line": int}
      * Read file content within workspace root
      * Path traversal protection: filepath.EvalSymlinks on BOTH target path
        AND workspace root before prefix comparison (prevents symlink escape)
      * Line range support (default: full file, max 500 lines)
    - WriteFile(ctx, args) (string, error):
      * args: {"path": string, "content": string}
      * RequiresApproval: true
      * Write file within workspace root
      * Path traversal protection on WRITE targets too: filepath.EvalSymlinks
        on the resolved parent directory before writing (prevents symlink-based
        escape where a symlink target is outside workspace root)
      * Create parent directories if needed
    - SearchFiles(ctx, args) (string, error):
      * args: {"pattern": string, "path": string, "max_results": int}
      * Regex search across files in workspace
      * Return matching lines with file:line format
      * Default max_results: 50
    - ListDirectory(ctx, args) (string, error):
      * args: {"path": string, "depth": int}
      * List directory contents within workspace
      * Default depth: 1 (no recursion)
    - RegisterFilesystemTools(registry *engine.ToolRegistry, wsRoot string):
      * Register all filesystem tools with the registry
      * Set workspace root for path resolution

    Test with temp directories: read existing, write new, search,
    list, path traversal rejection.
  </action>
  <verify>go test -v ./internal/tools/ -run TestFilesystem && echo "FILESYSTEM TESTS OK"</verify>
  <done>Filesystem tools read, write, search within workspace boundary</done>
</task>

<task id="T9" wave="4" depends_on="T7" complexity="simple">
  <files>internal/tools/terminal.go, internal/tools/terminal_test.go, internal/tools/research.go, internal/tools/research_test.go</files>
  <action>
    Build terminal and research tools:

    Terminal tool:
    - RunCommand(ctx, args) (string, error):
      * args: {"command": string, "args": []string, "timeout_seconds": int}
      * RequiresApproval: true (ALWAYS)
      * Execute via os/exec.Command with DISCRETE ARGUMENTS (no shell string
        interpretation -- prevents injection on both Windows and Unix)
      * Do NOT pass commands to cmd.exe/bash as a single string
      * Capture stdout + stderr combined
      * Default timeout: 30 seconds
      * Kill process on context cancellation
    - RegisterTerminalTools(registry, wsRoot string)

    Research tool:
    - SearchGrounding(ctx, args) (string, error):
      * args: {"query": string}
      * Send query to Gemini with search grounding enabled
      * Extract grounding metadata and citations
      * Format as: answer + sources list
    - RegisterResearchTools(registry, geminiClient *gemini.Client)

    Test: command execution with WINDOWS-FIRST commands (e.g., "cmd /c echo hello",
    "cmd /c dir"), timeout, cancellation. Do NOT use Unix-only commands (ls, cat)
    in test assertions -- this is a Windows-first application.
    Research tool with mock Gemini client.
  </action>
  <verify>go test -v ./internal/tools/ -run "TestTerminal|TestResearch" && echo "TERMINAL+RESEARCH TESTS OK"</verify>
  <done>Terminal executes commands with approval, research uses grounding</done>
</task>

<!-- ============================================================ -->
<!-- Wave 5: Session Persistence (SQLite)                        -->
<!-- ============================================================ -->

<task id="T10" wave="5" depends_on="T4,T5" complexity="moderate">
  <files>internal/engine/session.go, internal/engine/session_test.go</files>
  <action>
    Build session persistence with SQLite (pure Go, no CGO):

    - SessionStore interface:
      * SaveConversation(conv *Conversation) error
      * LoadConversation(id string) (*Conversation, error)
      * ListConversations(limit int) ([]ConversationSummary, error)
      * DeleteConversation(id string) error
    - ConversationSummary: ID, Title, TurnCount, CreatedAt, UpdatedAt
    - SQLiteSessionStore struct: implements SessionStore
      * NewSQLiteSessionStore(dbPath string) (*SQLiteSessionStore, error)
      * On init: PRAGMA journal_mode=WAL (prevents "database is locked" when
        engine writes while UI reads -- critical for desktop app concurrency)
      * Creates tables if not exist:
        - conversations (id, title, created_at, updated_at)
        - turns (id, conversation_id, role, content, tool_calls_json,
          tool_results_json, timestamp, token_count)
      * Uses modernc.org/sqlite (pure Go, no CGO required)
    - Save: db.BeginTx -> DELETE existing turns for conversation_id ->
      INSERT conversation (upsert) + INSERT all turns -> Commit
      (Delete-then-reinsert is simpler than per-turn diffing and handles
      turn edits/deletions correctly. Atomic transaction ensures no orphans.)
    - Load: SELECT conversation + JOIN turns ORDER BY timestamp
    - List: SELECT conversations ORDER BY updated_at DESC LIMIT N
    - Delete: DELETE conversation + CASCADE turns

    Test with in-memory SQLite: save/load round-trip, list ordering,
    delete cascade, concurrent access, turn update via delete-reinsert.
  </action>
  <verify>go test -v ./internal/engine/ -run TestSession && echo "SESSION TESTS OK"</verify>
  <done>Sessions persist to SQLite, survive application restart</done>
</task>

<!-- ============================================================ -->
<!-- Wave 6: Wails Integration (wire backend to frontend)        -->
<!-- ============================================================ -->

<task id="T11" wave="6" depends_on="T2,T4,T7,T8,T9,T10" complexity="complex">
  <files>cmd/gkestral/app.go, frontend/src/main.js</files>
  <action>
    Wire the engine to the Wails frontend:

    - cmd/gkestral/app.go -- extend App struct with Wails-bound methods
      (main.go is FROZEN from T1 -- all method additions go in app.go):
      * SendMessage(message string) -- spawns engine.Send in a GOROUTINE
        and returns immediately. CRITICAL: Must NOT block the Wails IPC
        thread or the frontend cannot process approval prompts (deadlock).
      * All Go-to-JS communication via runtime.EventsEmit (NO POLLING):
        - "stream:text" -- incremental response text
        - "stream:tool_call" -- tool call details
        - "stream:tool_result" -- tool execution result
        - "stream:thinking" -- thinking mode output
        - "stream:done" -- response complete
        - "stream:error" -- error occurred
        - "approval:request" -- tool needs user approval
      * App stores Wails context from OnStartup lifecycle callback
      * ApproveTool(callID string, approved bool) -- unblocks approval channel
      * LoadSession(id string) error
      * ListSessions() []ConversationSummary
      * NewSession() string -- create new conversation, return ID
      * GetWorkspaceInfo() WorkspaceInfo
      * SignIn() error -- trigger OAuth desktop flow
      * GetAuthStatus() AuthStatus
    - Use Wails Events API for real-time streaming:
      * Go backend emits events: "stream:text", "stream:tool", "stream:done"
      * Frontend listens via runtime.EventsOn()
    - frontend/src/main.js:
      * Wire send button via `window.go.main.App.SendMessage(msg)`
      * Listen for stream events via `runtime.EventsOn("stream:text", cb)`
      * Display tool calls with name + args
      * Show tool results (collapsible for long output)
      * Session list in sidebar (or top)
      * Auth status in top bar

    This task completes the end-to-end flow:
    User types -> Wails -> Engine -> Gemini -> Stream -> Wails Events -> UI

    Test: Wails build compiles, app struct methods callable.
  </action>
  <verify>go build ./cmd/gkestral/ && echo "WAILS BUILD OK"</verify>
  <done>End-to-end: type message, get streamed Gemini response with tool calls</done>
</task>

<!-- ============================================================ -->
<!-- Wave 7: Google Services Bridge + Auth UI                    -->
<!-- ============================================================ -->

<task id="T12" wave="7" depends_on="T11" complexity="moderate">
  <files>internal/tools/drive.go, internal/tools/drive_test.go, internal/tools/gmail.go, internal/tools/gmail_test.go</files>
  <action>
    Build Drive and Gmail tool bridges for the engine:

    Drive tools:
    - ImportFromDrive(ctx, args) (string, error):
      * args: {"query": string} or {"file_id": string}
      * Search Drive -> present results -> user selects -> download content
      * For Google Docs: export as plain text
      * For binary: download and report metadata
    - RegisterDriveTools(registry, driveClient *drive.DriveClient)

    Gmail tools:
    - ImportFromGmail(ctx, args) (string, error):
      * args: {"query": string, "max_results": int}
      * Search Gmail -> extract content -> format for context
      * Uses FormatForContext from gmail package
    - RegisterGmailTools(registry, gmailClient *gmail.GmailClient)

    Auth integration:
    - Both tools check for valid OAuth token before execution
    - If not authenticated: return "Please sign in with Google first"
    - Uses the auth.TokenRefresher for auto-refresh

    Test with mock Drive/Gmail clients: search, import, auth check.
  </action>
  <verify>go test -v ./internal/tools/ -run "TestDrive|TestGmail" && echo "DRIVE+GMAIL TESTS OK"</verify>
  <done>Drive and Gmail content importable via engine tool calls</done>
</task>

<!-- ============================================================ -->
<!-- Wave 8: Final Integration   |   Wave 9: Integration Tests   -->
<!-- ============================================================ -->

<task id="T13" wave="8" depends_on="T11,T12" complexity="moderate">
  <files>cmd/gkestral/app.go, cmd/gkestral/config.go</files>
  <action>
    Final integration and configuration:

    - Wire all components in app.go (main.go is FROZEN from T1):
      * Load config from environment + config file (~/.gkestral/config.json)
      * Create Gemini client with API key
      * Create engine with all tools registered
      * Create session store (SQLite at ~/.gkestral/sessions.db)
      * Detect workspace from current directory
      * Create Wails app with bound App struct
    - Config struct: APIKey, DefaultModel, ThinkingLevel, WindowWidth,
      WindowHeight, Theme, SessionDBPath
    - LoadConfig() -- env vars > config file > defaults
    - Error handling: show friendly error if API key missing
    - Graceful shutdown: save session on close

    Test: config loading with env overrides, default values.
  </action>
  <verify>go build ./cmd/gkestral/ && go test ./internal/... && echo "INTEGRATION OK"</verify>
  <done>Application starts, connects all subsystems, handles config</done>
</task>

<task id="T14" wave="9" depends_on="T13" complexity="simple">
  <files>internal/engine/integration_test.go, internal/tools/integration_test.go</files>
  <action>
    Integration tests for the full stack:

    All tests gated by `//go:build integration` tag.

    Engine integration tests:
    - TestIntegration_EngineStreamResponse: real Gemini streaming
    - TestIntegration_EngineToolCall: real tool dispatch + Gemini
    - TestIntegration_SessionRoundTrip: save/load on-disk SQLite

    Tool integration tests:
    - TestIntegration_FilesystemReadWrite: real file operations in temp dir
    - TestIntegration_TerminalCommand: real command execution
    - TestIntegration_DriveImport: real Drive API (needs token)
    - TestIntegration_GmailImport: real Gmail API (needs token)

    Env vars: GEMINI_API_KEY, GKESTRAL_TEST_TOKEN
  </action>
  <verify>go build -tags integration ./internal/... && echo "INTEGRATION BUILD OK"</verify>
  <done>Integration tests compile and skip gracefully without credentials</done>
</task>
</tasks>

<verification>
## Lint and Format Verification (T1 -- mandatory, wave-aware)

### Per-Wave Verification
```bash
# Always:
go vet ./...
go test ./internal/...

# Wave 2+ (engine exists):
go test -cover ./internal/engine/

# Wave 3+ (tools exist):
go test -cover ./internal/tools/

# Wave 1+ (workspace exists):
go test -cover ./internal/workspace/

# Maintained:
go test -cover ./internal/gemini/    # >= 85%
go test -cover ./internal/google/... # maintained
```

### Final Verification
```bash
go vet ./...
go test ./internal/...
go test -cover ./internal/engine/    # >= 80%
go test -cover ./internal/tools/     # >= 80%
go test -cover ./internal/workspace/ # >= 80%
go build ./cmd/gkestral/             # must compile
rg "api_key|apikey|secret" --type go internal/ cmd/ --glob "!*_test.go" || echo "CLEAN"
```
</verification>
